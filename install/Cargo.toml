[package]
name = "install"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
serde_with = "3.6"
anyhow = "1.0"
platform-dirs = "0.3.0"
chrono = { version = "0.4", features = ["std", "now"], default_features = false }